import styled from "styled-components";

export const Input = styled.input`
  outline: none;
  border: none;
  padding: 1rem 0.5rem;
  background: var(--background-card);
  display: block;
  color: var(--text-main);
`;
