import styled from "styled-components";

export const PostTitle = styled.h3`
  color: var(--text-main);
  font-size: 1.25rem;
  line-height: 1.6;
  font-weight: 500;
  transition: color 0.3s ease;
`;

export const PostTitlePrimary = styled.h1`
  color: var(--text-main);
  font-size: 1.25rem;
  line-height: 1.6;
  font-weight: 500;
`;
